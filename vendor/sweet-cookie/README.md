# sweet-cookie-local

Vendored browser cookie extraction helper for `slash-x`.

Supports:
- Inline payloads (JSON / base64 / file)
- Local browser reads: Chrome, Edge, Firefox, Safari on macOS

This package is not published. It exists so `slash-x` can use the patched Chrome cookie reader reliably on Edi's machines.
